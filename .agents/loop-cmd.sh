#!/bin/bash
# Agent: loop-cmd — the command gateway agent that triggers the recursive self-improving loop on request.
# /loop command — starts recursive self-improving loop
bash .agents/self-improving-agent.sh 0 loop-agent

