#logging

## Responsibility

Module functionality to be documented.

## Source Map

- `//! quantaradar-logging: lightweight async batched per-level file logger with rollover`

- `use std::path::{Path, PathBuf};`

- `use std::sync::Arc;`

- `use tokio::sync::Mutex;`

- `use tokio::time::{interval, Duration};`

## Dependencies

- `tokio`

- `chrono`

## Tests

- `cargo test -p <name>`


## Integration

- Part of the `logging` crate in the QuantRadar workspace
